The EOT version of the algorithm, everything else is the same. 

To find all changes, search for "TODO: change cosine similarity to EOT"

In total two changes, adding a function "_sinkhorn_entropic_cost_1d_hist", and replace the "old align_loss" function
